
    <div class="container">
      <div class="row" >
        <% @users.each do |user| %>
        <div class="col-md-4">
          <div class="container">
            <div class="mimage">
              <%= link_to image_tag(user.image.url(:display), width: "350"), user %>
            </div>
          </div>
        </div>
        <% end %>
      </div>
    </div>
