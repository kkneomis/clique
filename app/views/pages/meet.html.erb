<div class="container">    
  <div class="row">
        <% @users.each do |user| %>
            <div class="col-md-4">
                <%= link_to   image_tag(user.image.url(:display), width: "350"), user%> 
                
              <div class="mask">
                <h1><%= link_to user.name, user%></h1>
              </div>
          </div>

       <% end %>
    </div>
</div>